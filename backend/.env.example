DATABASE_URL=postgresql+psycopg://incidentiq:incidentiq@localhost:5432/incidentiq
CORS_ORIGINS=http://localhost:5173
# On Render, set CORS_ORIGINS to the exact frontend URL. This regex is a
# deployment-safe fallback for Render preview and production subdomains.
CORS_ORIGIN_REGEX=https://[a-z0-9-]+\.onrender\.com
JWT_SECRET_KEY=change-me
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
